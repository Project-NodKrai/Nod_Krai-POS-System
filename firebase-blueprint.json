{
  "entities": {
    "Store": {
      "title": "Store",
      "description": "Represents a user's store/business.",
      "type": "object",
      "properties": {
        "ownerId": { "type": "string", "description": "UID of the store owner" },
        "name": { "type": "string", "description": "Name of the store" },
        "subdomain": { "type": "string", "description": "Custom subdomain (e.g., storename.pos.n-e.kr)" },
        "bankName": { "type": "string" },
        "accountNumber": { "type": "string" },
        "qrCodeUrl": { "type": "string" },
        "allowCash": { "type": "boolean" },
        "hideOutOfStock": { "type": "boolean" },
        "lowStockThreshold": { "type": "number" },
        "isOpen": { "type": "boolean", "description": "Whether the store is currently open for business" },
        "currentSessionId": { "type": "string", "description": "ID of the current active business session" },
        "createdAt": { "type": "string", "format": "date-time" }
      },
      "required": ["ownerId", "name", "subdomain"]
    },
    "BusinessSession": {
      "title": "BusinessSession",
      "description": "A single business operation period from open to close.",
      "type": "object",
      "properties": {
        "storeId": { "type": "string" },
        "startTime": { "type": "string", "format": "date-time" },
        "endTime": { "type": "string", "format": "date-time" },
        "status": { "type": "string", "enum": ["open", "closed"] },
        "salesSummary": {
          "type": "object",
          "properties": {
            "cash": { "type": "number" },
            "card": { "type": "number" },
            "transfer": { "type": "number" },
            "total": { "type": "number" }
          }
        }
      },
      "required": ["storeId", "startTime", "status"]
    },
    "Product": {
      "title": "Product",
      "description": "An item for sale in the store.",
      "type": "object",
      "properties": {
        "storeId": { "type": "string" },
        "name": { "type": "string" },
        "category": { "type": "string" },
        "price": { "type": "number" },
        "cost": { "type": "number" },
        "stock": { "type": "number" },
        "minStock": { "type": "number" },
        "imageUrl": { "type": "string" }
      },
      "required": ["storeId", "name", "price", "stock"]
    },
    "Category": {
      "title": "Category",
      "description": "Product category.",
      "type": "object",
      "properties": {
        "name": { "type": "string" }
      },
      "required": ["name"]
    },
    "Sale": {
      "title": "Sale",
      "description": "A transaction record.",
      "type": "object",
      "properties": {
        "storeId": { "type": "string" },
        "items": { "type": "array" },
        "totalAmount": { "type": "number" },
        "receivedAmount": { "type": "number" },
        "changeAmount": { "type": "number" },
        "paymentMethod": { "type": "string", "enum": ["cash", "card", "transfer"] },
        "status": { "type": "string", "enum": ["pending", "completed", "cancelled"] },
        "type": { "type": "string", "enum": ["kiosk", "seller"] },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["storeId", "items", "totalAmount", "paymentMethod", "status", "timestamp"]
    }
  },
  "firestore": {
    "/stores/{storeId}": {
      "schema": "Store",
      "description": "Store configuration and metadata."
    },
    "/stores/{storeId}/products/{productId}": {
      "schema": "Product",
      "description": "Inventory items for a specific store."
    },
    "/stores/{storeId}/categories/{categoryId}": {
      "schema": "Category",
      "description": "Categories for a specific store."
    },
    "/stores/{storeId}/sales/{saleId}": {
      "schema": "Sale",
      "description": "Sales records for a specific store."
    },
    "/stores/{storeId}/businessSessions/{sessionId}": {
      "schema": "BusinessSession",
      "description": "Business operation sessions for a specific store."
    }
  }
}
